name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    
    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: qanda
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
    - uses: actions/checkout@v4
    
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'
        cache: 'npm'
    
    - name: Install dependencies
      run: npm ci
    
    - name: Check JavaScript syntax
      run: |
        node -c src/index.js
        node -c src/db/init.js
        node -c src/db/pool.js
        node -c src/routes/questions.js
        node -c src/websocket.js
    
    - name: Initialize database
      env:
        DATABASE_URL: postgresql://postgres:postgres@localhost:5432/qanda
      run: npm run init-db
    
    - name: Verify database schema
      env:
        DATABASE_URL: postgresql://postgres:postgres@localhost:5432/qanda
      run: |
        PGPASSWORD=postgres psql -h localhost -U postgres -d qanda -c "\dt"
        PGPASSWORD=postgres psql -h localhost -U postgres -d qanda -c "SELECT COUNT(*) FROM questions;"
        PGPASSWORD=postgres psql -h localhost -U postgres -d qanda -c "SELECT COUNT(*) FROM answers;"
